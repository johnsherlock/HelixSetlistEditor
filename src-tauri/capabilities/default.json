{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the Helix Setlist Editor.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
